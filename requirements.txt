# requirements.txt
# Python BDD Automation Framework - pinned dependencies
# Keep versions pinned for reproducible test environments.

# ------------------------
# Core BDD Framework
# ------------------------
behave==1.3.3                      # Behavior-driven development (Python) - BDD test runner
behave-html-pretty-formatter==1.16.1  # Pretty HTML formatter for Behave reports

# ------------------------
# Test runner & reporting
# ------------------------
pytest==9.0.2              # Flexible test runner
pytest-html==4.1.1         # HTML reporting for pytest
pytest-xdist==3.5.0        # Run tests in parallel

# ------------------------
# Web automation
# ------------------------
selenium==4.39.0           # Browser automation
webdriver-manager==4.0.2   # Auto-download browser drivers

# ------------------------
# API testing
# ------------------------
requests==2.32.5           # HTTP library for Python
requests-toolbelt==1.0.0   # Utilities for requests (multipart, streaming, etc.)

# ------------------------
# Allure reporting integrations
# ------------------------
allure-behave==2.15.3      # Allure plugin for behave
allure-pytest==2.15.3      # Allure plugin for pytest

# ------------------------
# Configuration management
# ------------------------
pyyaml==6.0.3              # YAML parsing
python-dotenv==1.2.1       # Load .env files into environment

# ------------------------
# Test data & utilities
# ------------------------
faker==40.1.0              # Generate fake test data
openpyxl==3.1.5            # Read/write Excel .xlsx files
pandas==2.3.3              # Data manipulation (optional, heavy dependency)
jsonschema==4.25.1         # Validate JSON payloads against schemas

# ------------------------
# Date/time utilities
# ------------------------
python-dateutil==2.9.0.post0  # Extended date/time parsing utilities

# ------------------------
# Logging, colors & networking
# ------------------------
colorlog==6.10.1           # Colored logging output
urllib3==2.6.2             # HTTP client (used by requests)
certifi==2026.1.4          # Mozilla CA bundle for TLS verification
charset-normalizer==3.4.4  # Encoding detection/normalization
idna==3.11                 # Internationalized Domain Names (IDNA) support

# ------------------------
# Notes
# ------------------------
# - Keep this file under version control.
# - To install: `pip install -r requirements.txt`
# - Update pins carefully and run your test-suite after changes.
